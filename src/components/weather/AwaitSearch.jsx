const AwaitSearch = () => {
  return (
    <div>
      <h1>To start, enter a city...</h1>
      <button onClick={()=>{console.log(process.env.APIKEY)}}>teste</button>

    </div>
  );
};

export default AwaitSearch;
